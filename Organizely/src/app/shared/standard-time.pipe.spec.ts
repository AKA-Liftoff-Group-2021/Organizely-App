import { StandardTimePipe } from './standard-time.pipe';

describe('StandardTimePipe', () => {
  it('create an instance', () => {
    const pipe = new StandardTimePipe();
    expect(pipe).toBeTruthy();
  });
});
